language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "Lireia"
  - git config user.email "qq6900023@live.cn"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${BLOG}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - blog-resource
env:
 global:
   - GH_REF: https://github.com/Lireia/Lireia.github.io
